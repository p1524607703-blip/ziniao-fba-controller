# 更新日志（CHANGELOG）

本项目所有值得记录的变更都会写在这里。格式参考 Keep a Changelog。

## [1.3.4] — 2026-09-15

### 新增

- **`watch_run.py` 增加紫鸟 Bridge 健康度监控**（`--bridge-url`，默认 `http://127.0.0.1:9481/health`）。
  Bridge 在负载下会「**端口仍在监听但服务假死**」，CLI 报「无法连接紫鸟浏览器 Bridge」，
  这是跑批变慢 / `步数超限` 判死的先兆——单看控制器状态完全看不出来，必须直接探它。
  每次打点附带探测：响应 >3s 告警「偏慢」；探测失败累计并告警；恢复时提示。
- **速率与 ETA**：每次打点输出「速率 x s/条 | 预计剩余 N 分钟 | 完成约 HH:MM（北京）」。
  ETA 统一按**北京时区**输出，与日志时间戳一致（此前用本机时区，同一行出现两个时区，
  看着像差 12 小时）。

### 修复

- **外部改动队列后看门狗会漏检新失败**（重要）：`failed` 按**下标游标**跟踪新增失败，
  跑批途中人工把误伤条目从 `failed` 捞回 `pending` 后列表变短，游标越界 →
  `failed[cursor:]` 恒为空切片 → **之后的新失败被整段忽略，额度守卫形同虚设**。
  现每轮检测游标越界即拉回并打日志；`base_fail` 同理下移。

## [1.3.3] — 2026-09-15

### 修复（重要：断电/强关会导致丢账，可能诱发重复提交）

处理流程是「**先出队 → 再处理 → 成功后写入 done**」：

```js
const item = state.pending.shift();   // 已从 pending 摘掉
state.inProgress = sku;
saveState();                          // ← 此刻磁盘上：pending 无它、done 无它
const r = await processOneSku(sku);   // 干活（含点击「继续」）
```

而 `saveState()` 以前只写 `pending/done/failed/config/throttle`，**`inProgress` 不落盘**；
`loadState()` 又强制 `state.inProgress = null`。所以：

- 若在处理中途**断电 / 强杀 / 直接关机**，这条 SKU 在磁盘上**彻底消失**——
  既不在 pending，也不在 done，对账时表现为「源表中从未进队列的 SKU」。
- 更糟的是它**可能已经提交成功**（Amazon 已生成问题编号）。本地无记录 →
  下次按源表重新入队时（`/api/bulk` 只对 pending+done 去重）会**再次提交 → 真·重复提交**。

修法：

- `saveState()` 增写 `inProgress` 与 `submitClicks`（崩溃后仍知道"这条点没点过提交"）。
- `loadState()` 新增**残留恢复**：磁盘若留着 `inProgress` 且该 SKU 不在 done/failed/pending，
  自动计入 `failed`，原因为「进程中断：已点击"继续"但未确认结果」或「进程中断：处理到一半未定论」，
  **禁止自动重提**，出表后落在「异常明细」页交人工核对。
  已实测两种情况：残留能被捞出 ✅ / 该 SKU 若其实已在 done 中则不重复计入 ✅。
- 点击「继续」时**先写一条 `IN_FLIGHT` 审计行**再点、并立即 `saveState()`：
  万一杀在点击之后，`submissions.csv` 里至少留得下"这条点过提交"的痕迹。

### 新增

- `export_report.py` 识别**在途**状态：跑批途中导出快照时，正在处理的那条已出队、未入 done，
  以前会让对账凭空少 1 条并误报「从未进队列」。现在把它作为第 4 个桶
  （`已提交 + 异常 + 待处理 + 在途 == 源表总数`）并在汇总页列出，
  停止原因相应显示「仍在运行中」。
- 汇总页新增「🔄 处理中（在途）」一行；「停止原因」行改为按内容动态定位加粗。

### 变更

- 异常分类调整：
  - 新增「进程中断（结果未知）」，建议「先到卖家后台核对是否已生成问题编号，确认未提交再重提」。
  - 「不符合重测资格（终态页）」的建议由"下月恢复后重试"改为
    **"先用探针法确认额度，确认可用再重试"**——该终态页也可能是残留页误读，别急着当月放弃。
  - 「页面结构未识别」不再附带"疑似同类额度问题"的猜测（未经验证）。

## [1.3.2] — 2026-09-15

### 新增

- **跑批看门狗 `watch_run.py`**：补上控制台熔断的一个缺口——控制台的
  `INFRA_FAIL_RE = /结构|网络|targetId|exec|步数超限|连续/` 只认基础设施类失败，
  而额度用尽返回的「不符合重新测量资格」被归为业务失败、**不触发熔断**，
  额度真耗尽时会一条条把整批跑成失败再收工。看门狗外挂三重守卫：
  - 额度守卫：连续 3 条「不符合资格」→ 自动暂停（核心）
  - 基础设施守卫：连续 5 条基础设施失败 → 自动暂停（兜底）
  - 停滞守卫：15 分钟无状态变化 → 只告警
  用 `--interval/--max-not-eligible/--max-infra/--stall-minutes` 可调，
  支持 `--out` 落日志。**必须用基线法**判断连续失败（state 里混着往批 failed）。

### 文档

- SKILL.md 新增「跑批看门狗」章节，并补一条踩坑：
  **起服务必须用后台任务方式**，`nohup node server.js &` 在工具调用结束时会被回收。
- 修正既有说明：**「亚马逊 FBA 重测每月上限 120 条」不是自然月硬上限**。
  实测 9 月自然月累计已 121 条成功提交仍在继续；9/11 那批
  「不符合重新测量资格」更可能是**陈旧残留页误读**而非额度用尽。
  → 判定额度改推**探针法**：解封关闭下跑 1 条，能推到「继续」硬停点即额度可用（且不消耗额度）。

## [1.3.1] — 2026-09-14

### 新增

- **`--since YYYY-MM-DD` 本轮/历史切分**：一个 `state.json` 里会同时躺着几个批次的记录，
  「已提交 45 条」分不清哪些是本轮干的。传入 `--since` 后自动按北京时间切分，
  汇总页多出「🚀 本轮新提交」「📦 其中历史已提交」两行，并在「已提交成功」页
  把历史行的问题编号标成 `—`、结果列注明「本轮之前，未重复提交」。
  省掉了过去靠人工事后补标注的步骤。
- **源表列自动识别**：运营给的清单常是「A 列款号 + B 列 FNSKU」两列形态，原先只读 A 列会整表读空。
  现在自动探测 FNSKU 所在列（按 `X[A-Z0-9]{9}` 形态命中数选列），并顺带把左侧款号带出来，
  在四个工作表里都追加一列**款号**——看款号比看 FNSKU 直观。显式传 `--column` 时仍以传入值为准。
- **停止原因动态化**：汇总页不再一律写「月度额度用尽」。三种情形分别给出：
  「本次已全部处理完毕」/「月度额度用尽（推定）」/「存在异常项」，
  避免"全跑完了却写着额度用尽"这类误导。

### 修复

- **单列源表下占位行列数错位**：当无待处理/无异常时补的占位行多出一格，
  与表头列数不一致（4 列表头配 5 列占位行）。改为按是否带款号列动态拼装，列数严格对齐。
- **高亮染错列**：新增款号列后，「状态」「异常类型」列位置右移一格，
  而着色仍硬编码 `row[2]`，导致红色/黄色标记落在**款号**列而不是状态列。
  改为按 `col_status` / `col_kind` 动态取列。
- **全待处理时崩溃**：`done` 与 `failed` 都为空时 `max()` 对空序列取最大会抛 `ValueError`，
  现在回退为 `—`（`bj()` 同步加了空值保护，不再误显示 1970 年）。
- 表格末尾补一空行/空页的友好占位（「本次无剩余未提交项」/「本次无异常项」），
  空结果不再是一张没有任何提示的空白页。

### 文档

- SKILL.md 补充源表两种形态说明与款号列行为；导出命令示例更新为双列源表。
- SKILL.md 新增「换批次 / 换运营组」章节：改 `state.json` 前必须先停服务，
  旧队列务必备份；看门狗须用**基线法**判断连续失败（state 混批次会误触发熔断）。

## [1.3.0] — 2026-09-10

### 新增

- **提交问题编号（case ID）采集**：提交成功后从页面「已创建问题 21984441071」提取编号，
  写入 `state.done[].caseId` 并落盘 `submissions.csv` 审计日志。点击"继续"前先用 `preSubmitDims`
  留档尺寸/重量（提交后页面会跳走读不到）。
- **月度额度用尽识别**：亚马逊 FBA 重测每月上限 120 条，额度用尽后返回**无任何按钮**的终态页。
  改为读取工作流步骤名 `[data-step-attr] → currentStepName`，命中
  `inform_seller_not_eligible_for_re_measurement` 即判 `NOT_ELIGIBLE`，并记录页面展示的包裹尺寸/重量。
- **陈旧残留页防护**（根因修复）：终态页增加 FNSKU 一致性校验，不一致返回 `STALE_PAGE`，
  服务端强制 `location.reload()` 重来（最多 3 次）。
- **结果对账表导出** `export_report.py`：与源 FNSKU 清单逐条对账，产出
  汇总 / 已提交成功 / 未提交-待重排 / 异常明细 四页工作簿；内置对账自检，数字不闭合直接报错退出。
- **最终提交解封开关** `ALLOW_SUBMIT`：默认 false（硬停在"继续"前），
  支持环境变量 `ZINIAO_ALLOW_SUBMIT=1` 或 `POST /api/allow-submit` 开启，重启自动复位。

### 修复

- **整段 SKU 被误判为"不符合资格"**：旧的就绪判断是"页面有内容就算就绪"，
  而上一条 SKU 的终态页同样有内容，导致本条一开工就读到上一条的结论。
  改为必须确认起始步骤 `obtain_fnsku_for_us_...` 才开工，否则强制重载（最多 3 次）。
- **虚增成功数 / 串 case 号**：新增数据完整性闸门——某 SKU 从未点过"继续"却出现成功页时，
  判定为残留页并重载重来，绝不记为提交成功。
- **就绪等待改为轮询探测**：替换原先盲等 6s（慢渲染时不够、快渲染时浪费）。
- **`UNEXPECTED_STATE` 重试策略**：从"仅 1 次等 5s"改为递增退避 3 次（5/10/15s），
  第 2 次起重新做就绪探测，避免在未渲染的页面上空转到失败。
- **连续误判熔断**：连续 4 条同因基础设施类失败自动暂停（业务类如"无库存"不触发）。
- `markFailed()` 支持附加字段，便于留档页面尺寸/重量等诊断信息。

### 文档

- SKILL.md 新增「排障第一步：月度额度用尽」章节（含终态页特征与 DOM 元数据判定方法）。
- SKILL.md 新增终态/异常状态一览表、case ID 采集、解封开关、对账表导出三节。
- 新增本 CHANGELOG.md。

## [1.2.1] — 2026-08-25

### 新增

- 标注跨平台支持：macOS / Linux / Windows 均可用，SKILL.md 与 README 同步说明。

### 修复

- CLI 跨平台解析：Windows 上 `ziniao-cli` 启动器名是 `ziniao-cli.cmd`，
  改为按 `process.platform === 'win32'` 选择，并给 `spawn` 加 `shell` 以解析 `.cmd`。

## [1.2.0] — 2026-08-25

### 新增

- 最终提交解封确认流程说明（Agent 询问授权的安全约定）。

## [1.0.0] — 2026-08-25

- 首个公开版本：零依赖 Node 控制台，逐 SKU 推进到"继续"按钮前硬停，
  带开始/暂停开关、准备/完成双列表、三档调速（防封控）。
